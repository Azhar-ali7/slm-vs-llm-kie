# Secrets — never commit API keys
.env
*.env
!.env.example

# Data & results (large / regenerable)
data/raw/
data/processed/
# Regenerable SFT data (scripts/make_train_data.py)
data/train/
# Phase-3 GGUF + Modelfile downloaded from Kaggle
artifacts/
results/runs.jsonl
results/summary.csv
results/*.png
results/REPORT.md

# Frozen evidence snapshots ARE versioned (dissertation record) — exempt them
!results/snapshots/
!results/snapshots/**

# Generated report artifacts (keep the directory, ignore outputs)
report/*.docx
report/*.pdf
report/figures/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# Office temp lock files (e.g. ~$Deck.pptx)
~$*

# OS
.DS_Store
._*
.Spotlight-V100
.Trashes
results/*.log
