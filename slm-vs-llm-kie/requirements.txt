# Core
pydantic==2.9.2
pyyaml==6.0.2
requests==2.32.3
pandas==2.2.3
numpy==2.1.3

# Models / APIs
openai==1.54.4          # AzureOpenAI client (GPT models / frontier slot)
azure-ai-inference==1.0.0b9   # Azure AI Foundry serverless (Phi/Mistral/Llama)
tenacity==9.0.0         # retry with backoff
tiktoken==0.8.0         # token estimate / fallback counting

# Efficiency measurement (M1: psutil only — pynvml is NVIDIA-only, omitted)
psutil==6.1.0

# Documents / raw PDF (optional preprocessing path)
pdfplumber==0.11.4

# Analysis & reporting
matplotlib==3.9.2
tabulate==0.9.0         # pandas .to_markdown() for REPORT.md
rich==13.9.4            # projector-friendly demo output

# Tests
pytest==8.3.3
