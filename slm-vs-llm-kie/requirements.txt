# Core
pydantic==2.9.2
pyyaml==6.0.2
requests==2.32.3
pandas==2.2.3
numpy==2.1.3

# Models / APIs
openai==1.54.4          # AzureOpenAI client for the API models
tenacity==9.0.0         # retry with backoff
tiktoken==0.8.0         # token estimate / fallback counting

# Efficiency measurement (M1: psutil only — pynvml is NVIDIA-only, omitted)
psutil==6.1.0

# Documents / raw PDF (optional preprocessing path)
pdfplumber==0.11.4

# Analysis & reporting
matplotlib==3.9.2
python-docx==1.1.2      # BITS Mid-Sem .docx report
rich==13.9.4            # projector-friendly demo output

# Tests
pytest==8.3.3
