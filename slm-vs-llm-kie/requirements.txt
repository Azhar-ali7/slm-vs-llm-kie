# Core
pydantic==2.9.2
pyyaml==6.0.2
requests==2.32.3
pandas==2.2.3
numpy==2.1.3

# Models / APIs
openai==1.54.4          # OpenAI-compatible client (OpenRouter + Azure OpenAI)
httpx==0.27.2           # pinned: openai 1.54 breaks on httpx>=0.28 (proxies kwarg)
azure-ai-inference==1.0.0b9   # Azure AI Foundry serverless (optional path)
tenacity==9.0.0         # retry with backoff
tiktoken==0.8.0         # token estimate / fallback counting

# Efficiency measurement (M1: psutil only — pynvml is NVIDIA-only, omitted)
psutil==6.1.0

# Documents / raw PDF (optional preprocessing path)
pdfplumber==0.11.4

# Analysis & reporting
matplotlib==3.9.2
tabulate==0.9.0         # pandas .to_markdown() for REPORT.md
rich==13.9.4            # projector-friendly demo output
python-docx==1.1.2      # scripts/make_report_doc.py -> Mid_Sem_Report.docx
python-pptx==1.0.2      # scripts/make_slides.py -> mid-sem review deck

# Tests
pytest==8.3.3
